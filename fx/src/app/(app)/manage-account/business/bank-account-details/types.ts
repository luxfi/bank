export interface IBankAccount {
  name: string;
  branch: string;
  accountHolder: string;
  country: string;
  sortCode: string;
  accountNumber: string;
  iban: string;
  bic: string;
  currency: string;
}
